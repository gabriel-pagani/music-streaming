CREATE MASTER KEY ENCRYPTION BY PASSWORD = 'Sua_Senha_Forte_Complexa';

ALTER MASTER KEY ADD ENCRYPTION BY SERVICE MASTER KEY;

CREATE CERTIFICATE CERTIFICADO WITH SUBJECT = 'Certificado para proteção de dados';

CREATE SYMMETRIC KEY CHAVE WITH ALGORITHM = AES_256 ENCRYPTION BY CERTIFICATE CERTIFICADO;
